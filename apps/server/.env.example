# Node Environment (development, production)
NODE_ENV=development

# Database URL for SQLite (mapped relative to prisma directory)
DATABASE_URL="file:../../data/worksos.db"

# Note: Backend port is strictly bound to 3080 per AGENTS.md rules.
